## Packages
date-fns | Formatting timestamps for statuses, chat, and announcements
clsx | Class name merging
tailwind-merge | Class name merging

## Notes
The application is wrapped in a mobile-simulator container to enforce the mobile-first design even on desktop browsers.
